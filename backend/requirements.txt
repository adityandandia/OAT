fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1

# Database
sqlalchemy==2.0.36
pyodbc==5.2.0

# Auth
pyjwt==2.10.1
passlib[bcrypt]==1.7.4
python-multipart==0.0.20

# Env management
python-dotenv==1.0.1

# CORS (needed since frontend runs on a different port - Vite default 5173)
# already included via fastapi, just needs middleware import, no separate package